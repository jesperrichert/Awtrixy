info:
  name: Delete Custom App
  type: http
  seq: 2

http:
  method: POST
  url: "{{AWTRIX_WEB_URL}}/api/custom?name=bruno"
  params:
    - name: name
      value: bruno
      type: query
  auth:
    type: basic
    username: "{{AWTRIX_USER}}"
    password: "{{AWTRIX_PASSWORD}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
