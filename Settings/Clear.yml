info:
  name: Clear
  type: http
  seq: 3

http:
  method: POST
  url: "{{AWTRIX_WEB_URL}}/api/resetSettings"
  auth:
    type: basic
    username: "{{AWTRIX_USER}}"
    password: "{{AWTRIX_PASSWORD}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
