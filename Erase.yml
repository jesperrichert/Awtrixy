info:
  name: Erase
  type: http
  seq: 7

http:
  method: POST
  url: "{{AWTRIX_WEB_URL}}/api/erase"
  auth:
    type: basic
    username: "{{AWTRIX_USER}}"
    password: "{{AWTRIX_PASSWORD}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
