info:
  name: Loop
  type: http
  seq: 1

http:
  method: GET
  url: "{{AWTRIX_WEB_URL}}/api/loop"
  auth:
    type: basic
    username: "{{AWTRIX_USER}}"
    password: "{{AWTRIX_PASSWORD}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
